# Oxelia51 任务分工与协作规则

**版本**：v3.0 | **日期**：2026-07-27

---

## 0. 每次任务前

收到任务后，逐项确认：

1. **任务分类**——属于哪个领域？
   - 后端/数据库/API/Go 代码 → 后端任务
   - 前端/CSS/布局/动画/React → 前端任务
   - 审查/测试/文档/命名一致性 → 审查任务
   - 架构决策/任务分解/部署/服务器操作/CI/CD → 架构任务
   - 跨域紧急修复（≤5 行） → 架构任务，事后记录

2. **环境认知**——涉及服务器或部署时，先查 `deploy/README.md`

3. **行为边界**——不得：
   - 在非自己领域内实现大型功能
   - 跳过审查直接部署
   - 不确定时猜测

---

## 1. 任务分类与边界

### 后端任务

**职责**：Go 业务逻辑、API handler、service 层、repository、数据库访问、认证、中间件、定时任务、性能优化。

**修改范围**：
- `backend/**/*.go`
- `proxy-gateway/**/*.go`
- `analytics/**/*.{cpp,h}`
- `migrations/*.sql`

**不得修改**：前端代码、UI、CSS、部署配置（deploy/）、CI/CD。

**上报**：数据库模式变更、API 契约变更、框架变更、新依赖引入。

### 前端任务

**职责**：React 组件、页面、路由、状态管理、CSS、响应式、动画。

**修改范围**：
- `desktop/ui/src/**/*.{ts,tsx,css}`
- `langfuse-token/web/src/**/*`

**不得修改**：后端代码、API 定义、数据库、部署配置。

**上报**：API 字段缺失、全局导航变更、主题系统变更。

### 审查任务

**职责**：代码审查、测试、文档、CHANGELOG、README、死代码检测。

**修改范围**：
- `**/*.md`
- `**/*_test.go`
- `**/*.test.*`

**不得修改**：业务源代码、UI 实现、架构、数据库模式。

**输出**：[致命/严重/建议] 分级报告，标注需裁定项。

### 架构任务

**职责**：需求分析、任务分解、技术选型、最终集成、Git 提交推送、SSH 部署、CI/CD。

**修改范围**：
- 全部文件（协调各领域）
- `deploy/**`、`.github/**`、`scripts/**`
- `docs/`（架构文档）

**不得**：替代后端/前端实现大型功能、跳过审查推送。

---

## 2. 开发流程

```
需求分析（架构任务）
  → 任务分解（架构任务）
  → 后端/前端实现（各自任务）
  → 构建验证：go build / npm run build
  → 审查（审查任务）
  → 架构裁定 + 修复
  → Git 提交 + 推送
  → 部署（GitHub Actions 编译 → Webhook → 服务器）
```

| 阶段 | 领域 | 产出 |
|:--:|------|------|
| 1 | 架构 | 需求理解 + 影响分析 |
| 2 | 架构 | 任务拆解 + 提示词 |
| 3 | 后端/前端 | 代码实现 |
| 4 | 架构 | 构建验证 |
| 5 | 审查 | 分级审查报告 |
| 6 | 架构 | 裁定 + 修复分配 |
| 7 | 架构 | git push + 部署 |

---

## 3. 编码规则

1. **先读后写**——修改前完整阅读文件
2. **一致性优先**——匹配现有命名、格式、风格
3. **最小变更**——只改必要内容
4. **复用优先**——搜索已有实现
5. **小函数**——一个函数一个职责
6. **有意义命名**——不用 tmp/data/test
7. **无魔法值**——用命名常量
8. **不忽略错误**——始终处理
9. **不保留死代码**
10. **自我审查**

### 3.1 字体约定

- 页面级标题：`var(--sans)` (Inter)
- 内嵌板块标题：`var(--heading)` (Noto Serif SC)
- 等宽/数字：`var(--mono)` (JetBrains Mono)
- 正文：`var(--sans)`
- 不引入新字体族，变更需架构裁定

---

## 4. Git 工作流

**提交格式**：`feat:` / `fix:` / `refactor:` / `docs:` / `test:` / `style:` / `chore:`

**合并规则**：仅架构任务执行最终合并。合并前确保：构建通过、测试通过、审查完成。

---

## 5. 禁止事项

- 不发明需求——缺失时确认
- 不修改无关代码
- 不忽略现有约定
- 不引入不必要依赖
- 不过度工程化
- **不在服务器上编译**——所有构建（go build、npm run build、make、cmake）在本地完成，服务器只做部署
- 不隐藏问题
- 不留下临时方案
- 不跳过测试
- 不跳过文档
- 不未经验证称完成

---

## 6. 黄金法则

1. 架构先于实现
2. 正确性先于速度
3. 可维护性先于技巧
4. 一致性先于个人偏好
5. 简洁方案优先
6. 沟通先于假设
7. 文档先于完工
8. 测试先于合并
9. 审查先于发布
10. 不确定时停止——不猜测

## 7. 优先级

1. 人类开发者指令
2. 本规范 (AGENTS.md)
3. CLAUDE.md
4. 项目文档
5. 默认行为
