# 契约审查修订记录

**当前版本**：v2.0  
**最后更新**：2026-07-08  

---

## v1.1 审查修订（Codex → v1.1，2026-07-05）

**修订执行**：Cursor 契约主笔  
**基线**：v1.0 → **v1.1（已重新冻结）**

---

## P0 处理结果

| # | 审查项 | 处理 |
|---|--------|------|
| 1 | C1 `visitor_visible` 语义错误 | 重命名为 **`user_accessible`**，全文档替换 |
| 2 | D1 Nginx `/api/` 冲突 | ADR-006：阶段 0～1 公网仅 DormGuard；平台 `:8080` 本地/SSH |
| 3 | D2/D5/M3 DormGuard 认证 | ADR-007：`OXELIA_GATEWAY_MODE`；双账号共存；不合并 root→oxelia51 |

## P1 处理结果

| # | 审查项 | 处理 |
|---|--------|------|
| 4 | M1 JWT 生命周期 | platform-api：access 7d、refresh 30d、logout、黑名单 |
| 5 | C4 管理 API `:id` | 统一为 **`/api/admin/tools/:slug`** |
| 6 | C2 忘记密码限流 | 07 摘要 + mailer 独立计数 |
| 7 | C3 `online_capable` 默认 | 缺失默认 **`false`**；分在线/作品集 manifest 示例 |
| 8 | 登录限流 | 升级为契约级要求 |
| 9 | M4 数据库关系 | ADR-007：DormGuard 独立 MySQL |

## P2 处理结果

| # | 审查项 | 处理 |
|---|--------|------|
| 10 | M6 邮箱唯一 | `email` UNIQUE + `409 EMAIL_TAKEN` |
| 11 | M5 Token 规范 | Redis、24h、单次、`TOKEN_EXPIRED` |
| 12 | C5 阶段 1 状态 | ADR-007 §6 明确三字段取值 |
| 13 | M2 404/502 | gateway-contract 错误码表 |
| 14 | D3 CORS | ADR-007：增加 `:5173` |

## P3 处理结果

| # | 审查项 | 处理 |
|---|--------|------|
| 15 | M7 徽章映射 | platform-api `badge` 字段 + tool-registration 表 |
| 16 | M8 作品集 | 独立 `portfolio_items` 表 |

## 未采纳 / 延后

| 项 | 说明 |
|----|------|
| refresh 是否本期必做 | **本期实现**（审查建议采纳） |
| DormGuard `root` 废弃 | **不采纳**；现网保留至切换日 |

## 解冻条件

Trae 页面 Agent 可在 **v1.1 冻结** 后启动；Cursor 优先 ADMIN-01、AUTH-01、DG-INT-01（DormGuard 网关模式）。

---

## REV-04 闭环（2026-07-05）

| 项 | 结果 |
|----|------|
| 审查员 | Codex + DeepSeek V4 Pro |
| 结论 | **通过**（16/16 已闭环；交叉检查 A～E 通过） |
| P3 残余 | refresh 存储已修订为 Redis（platform-api §6） |
| Cursor 放行 | ADMIN-01、AUTH-01、TOOL-01、GW-01 |
| Trae 放行 | UI-01～04 可并行 |
| 需 v1.2 | 否 |

---

## v2.0 变更记录（2026-07-07 ～ 2026-07-08）

### 2026-07-07

| 变更项 | 说明 | 涉及文档/代码 |
|--------|------|--------------|
| AGENTS.md v2.0 | 多 Agent 模型从 3-agent 升级为 **5-agent 模型**：Codex（架构）、Cursor（后端）、Hermes（前端）、Qoder（QA 与部署）、Trae Work（审查与知识） | `AGENTS.md`、`docs/06-多Agent任务板.md`、`.trae/rules/project_rules.md` |
| account_id 账号体系 | `username` 拆分为不可变 `account_id` + 可修改显示名 `username`；新增 `PATCH /api/auth/profile` | `backend/internal/handler/auth.go`、`backend/internal/model/user.go`、`backend/migrations/009_account_id.*` |
| Friends / Profile 页面 | 新增 `/friends` 友情链接页、`/profile` 个人资料页 | `frontend/src/pages/Friends.*`、`frontend/src/pages/Profile.*` |
| DormGuard QQ Bot | QQ 机器人已连接上线 | DormGuard 仓库 bot/ 模块 |

### 2026-07-08

| 变更项 | 说明 | 涉及文档/代码 |
|--------|------|--------------|
| 服务器资源监控面板 | 新增服务器资源监控面板 | `backend/internal/monitor/`、`frontend/src/pages/Admin*（监控 tab）` |
| 文档清理 | `docs/01~04` 归档至 `docs/archive/`；删除 REV-02/03 report 重复版、REV-04 残篇；README 与 project_rules 同步 v2.0 信息 | `docs/archive/`、`docs/reviews/`、`README.md`、`README_CN.md`、`.trae/rules/project_rules.md` |

### v2.0 审查结论

| 项 | 结果 |
|----|------|
| 审查员 | Trae Work + Kimi K2.7 Code |
| 报告 | [REV-05 报告](reviews/REV-05-task-batch-report.md) |
| 结论 | **有条件通过** —— 无 P0 问题，3 项 P1 修复后准予进入下一阶段 |
| 状态 | 文档已同步，待 P1 修复后最终合并 |
